<!DOCTYPE html>
<html lang="en">
  <head>
    <title>Donovan C. Young</title>
    <%= stylesheet_link_tag    "application", media: "all" %>
    <%= javascript_include_tag "application" %>
    <%= csrf_meta_tags %>
    <%= render 'layouts/shim' %>
  </head>

  <body>
    <div class="container">
      <div class="row span12">
        <header>
          <div id="text-logo"> <%= page_heading( yield(:title) ) %> </div>
        </header>

        <div class="row">
          <div class="span10">
            <% unless yield(:subtitle).empty? %>
              <div class="body-header"><%= yield(:subtitle) %></div>
            <% end %>
            <div class="row"> <%= yield %> </div>
          </div>
          <div class="span2"> <%= render 'layouts/sidebar' %> </div>
        </div>
      </div>
      <div class="row"> <%= render 'layouts/footer' %> </div>
    </div>
  </body>
</html>
