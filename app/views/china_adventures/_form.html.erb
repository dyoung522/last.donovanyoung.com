<%= form_for(@china_adventure) do |f| %>
  <% if @china_adventure.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@china_adventure.errors.count, "error") %> prohibited this china_adventure from being saved:</h2>

      <ul>
      <% @china_adventure.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :date %><br />
    <%= f.date_select :date %>
  </div>
  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :content %><br />
    <%= f.text_area :content %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
